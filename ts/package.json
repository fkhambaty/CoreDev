{
  "name": "agent-lab-scorer",
  "version": "0.1.0",
  "description": "TypeScript CLI that scores agent trajectories against the eval rubric.",
  "type": "module",
  "bin": {
    "score-trajectory": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "score": "tsx src/cli.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
